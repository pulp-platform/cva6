[project]
name = "cva6tb"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "requests",
    "hjson",
    "mako",
    "pyyaml",
    "tabulate",
    "yapf",
    "mkdocs",
    "mkdocs-material",
    "markdown-grid-tables",
    "tclint==0.4.2",
    "flatdict>=4.1.0",
    "setuptools<81", # Required by old `regtool`
]

[tool.uv]
package = false
